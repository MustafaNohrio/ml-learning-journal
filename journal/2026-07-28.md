# July 28, 2026

## Studied & Practiced

- Learned how to select data from a Pandas DataFrame using `df['col']` and `df[['col1', 'col2']]`.
- Practiced label-based indexing with `.loc[]` and position-based indexing with `.iloc[]`.
- Used boolean masking to filter rows based on conditions.
- Learned to filter data using `.query()` with readable expressions.
- Practiced selecting and filtering data from datasets using different techniques.

## Confused Me

- Choosing between `.loc[]`, `.iloc[]`, and `.query()` for different situations took some practice.

## Tomorrow's Plan

- Learn data aggregation using `.groupby()` and `.agg()`.
- Explore `.value_counts()`, `.sort_values()`, and `.nlargest()`.
- Practice summarizing and analyzing datasets using aggregation techniques.