# July 23, 2026

## Studied & Practiced

- Learned NumPy statistical functions: `mean()`, `std()`, `var()`, `min()`, `max()`, `percentile()`, and `corrcoef()`.
- Explored the `axis` parameter to perform operations row-wise and column-wise.
- Practiced finding statistical measures on both 1D and 2D arrays.
- Learned how correlation measures the relationship between two datasets.

## Confused Me

- Understanding when to use `axis=0` and `axis=1` in different NumPy functions took some practice.

## Tomorrow's Plan

- Learn NumPy's random module.
- Explore `rand()`, `randn()`, `randint()`, and `random.seed()`.
- Practice generating reproducible random numbers for simulations and testing.