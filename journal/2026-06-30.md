# June 30, 2026

## Studied & Practiced

- Learned exception handling using `try`, `except`, and `raise`.
- Handled `ZeroDivisionError`, `ValueError`, and general exceptions.
- Practiced `filter()`, `map()`, and `reduce()` with lambda functions.
- Used `match-case` to build a menu-driven calculator.

## Built

- Calculator with exception handling.
- Programs using `filter()`, `map()`, and `reduce()`.

## Confused Me

- Knowing when to use specific exceptions instead of the general `Exception` class.

## Tomorrow's Plan

- Learn file handling.
- Practice reading from and writing to files.