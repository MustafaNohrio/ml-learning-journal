# July 29, 2026

## Studied & Practiced

- Learned how to summarize and analyze data using Pandas aggregation functions.
- Used `.groupby()` to group data by categories.
- Applied `.agg()` to calculate statistics such as mean, sum, min, max, and count.
- Counted unique values using `.value_counts()`.
- Sorted data using `.sort_values()`.
- Retrieved the top records using `.nlargest()`.
- Practiced analyzing datasets using different aggregation techniques.

## Confused Me

- Understanding when to use `.groupby().agg()` instead of applying aggregation functions directly to a DataFrame.

## Tomorrow's Plan

- Learn how to handle missing data in Pandas.
- Practice using `.isnull()`, `.fillna()` (mean, median, `ffill`, `bfill`), and `.dropna()`.
- Apply missing data handling techniques on real datasets.