# August 6, 2026

## Studied & Practiced

- Started learning **Matplotlib**, Python's core library for data visualization.
- Learned the difference between a **Figure** and **Axes**.
- Created basic line plots using `plt.plot()`.
- Added titles and axis labels using `plt.title()`, `plt.xlabel()`, and `plt.ylabel()`.
- Customized plots with different colors, line styles, markers, and line widths.
- Created figures with custom sizes using `plt.figure(figsize=(...))`.
- Saved plots as PNG images using `plt.savefig()` before displaying them with `plt.show()`.

## Confused Me

- Understanding when to use the object-oriented approach (`Figure` and `Axes`) instead of the simpler `pyplot` functions.

## Tomorrow's Plan

- Plot my Week 5 marks as a line chart.
- Practice adding labels, legends, grids, and different plot styles.
- Explore creating multiple plots in a single figure.