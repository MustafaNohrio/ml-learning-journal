# July 30, 2026

## Studied & Practiced

- Learned how to identify missing values using `.isnull()`.
- Counted missing values in each column with `.isnull().sum()`.
- Filled missing values using `.fillna()` with a constant, mean, median, forward fill (`ffill`), and backward fill (`bfill`).
- Removed missing data using `.dropna()` for rows, columns, and rows where all values are missing.
- Practiced handling missing data on a sample dataset.

## Confused Me

- Deciding when to use `fillna()` versus `dropna()` and choosing the most appropriate filling method for different situations.

## Tomorrow's Plan

- Learn how to combine DataFrames using `merge()`, `join()`, and `concat()`.
- Practice merging datasets using different types of joins.
- Understand when to use each method.