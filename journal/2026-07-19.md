# July 19, 2026

## Studied & Practiced

- Learned how Python virtual environments (`venv`) work.
- Created a virtual environment and installed practice packages.
- Generated a `requirements.txt` file using `pip freeze`.
- Recreated the environment by installing dependencies from `requirements.txt`.
- Understood why `requirements.txt` is used instead of sharing the `venv` folder.

## Confused Me

- Understanding the difference between activating an existing virtual environment and recreating it from `requirements.txt` took some practice.

## Tomorrow's Plan

- Start learning NumPy.
- Explore NumPy arrays and basic array operations.
- Compare Python lists with NumPy arrays.