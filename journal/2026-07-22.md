`   # July 22, 2026

## Studied & Practiced

- Performed element-wise array operations (`+`, `-`, `*`, `/`, `**`) in NumPy.
- Learned how broadcasting works and how NumPy handles arrays with compatible shapes.
- Practiced reshaping, transposing, and flattening arrays using `reshape()`, `transpose()`, and `flatten()`.
- Learned the difference between element-wise multiplication and matrix multiplication.
- Implemented matrix multiplication manually and verified the result using NumPy's `@` operator.

## Confused Me

- Understanding the rules of broadcasting and remembering when two arrays are compatible for operations.

## Tomorrow's Plan

- Learn NumPy aggregation functions.
- Explore statistical operations on arrays.
- Practice solving more array-based problems.