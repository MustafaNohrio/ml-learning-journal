# July 24, 2026

## Studied & Practiced

- Explored the NumPy random module, including `np.random.seed()`, `rand()`, `randn()`, and `randint()`.
- Learned why random seeds are important for generating reproducible results.
- Simulated 10,000 coin flips using NumPy and observed the Law of Large Numbers as the sample mean approached 0.5.
- Installed Pandas and generated a `requirements.txt` file using `pip freeze` to capture project dependencies.

## Confused Me

- Understanding the difference between `rand()` and `randn()` and when each should be used.

## Tomorrow's Plan

- Start learning Pandas.
- Create and work with Series and DataFrames.