# August 1, 2026

## Studied & Practiced

- Learned how to combine DataFrames using `pd.merge()` and `pd.concat()`.
- Practiced different types of joins: `inner`, `left`, `right`, and `outer`.
- Concatenated DataFrames both row-wise and column-wise using `pd.concat()`.
- Merged two datasets and performed grouping to count records by department.
- Calculated the average marks for each department after merging the data.

## Confused Me

- Remembering when to use each type of join (`inner`, `left`, `right`, and `outer`) depending on the desired output.

## Tomorrow's Plan

- Revise all Pandas topics covered this week.
- Solve practice exercises combining data selection, aggregation, missing data handling, and DataFrame merging.
- Strengthen understanding of the complete Pandas workflow.